```markdown
# 데일리 리포트 — 2026-07-21 (박성은)

## 오늘 한 일
- `urgent_flags`(미성년자 성착취·실제 위해 예고 등) 감지 시 카테고리·차수 불문 즉시 영구 이용제한(level 4)으로 강제하고, HITL 대기 없이 즉시 제재하도록 처리 흐름을 수정했다.
- 제재 안내 우편 본문(`mail_content`)을 AI 호출 없이 코드에서 직접 조립하는 `build_mail_content` 함수를 추가했다. 또한 `channel` 파라미터를 추가해 음성/채팅 채널을 우편 본문에 구분해 표기하도록 했다.
- RAG 검색 `top_n`을 8에서 12로 상향 조정하고 진단 스크립트(`rag_diagnose.py`, `rag_test.py`) 및 결과 파일을 갱신했다. `top_n=12` 적용 후 오탐 케이스(욕설강도 3→2) 2건이 정답으로 교정되었음을 진단 결과로 확인했다.
- `docs/decisions.md`에 RAG `top_n` 변경 및 제재 상한값 결정 근거를 기록했다.

## 배운 점 / 특이사항
- `top_n` 증가로 문서량이 늘어나면서 규정 문서 내 구체적 예시 문장까지 함께 검색되어 단계 판정 정확도가 올라갔다. 문서량 vs. 비용·지연 간 균형을 계속 주시해야 한다.
- `urgent_flags` 처리 시 confidence가 낮아도 HITL로 미루지 않도록 분기를 명시적으로 설정한 점이 정책(4·11절) 준수에 중요했다.

## 내일 할 일
- (Day 5) 평가지표 1회 측정: 카테고리 정확도·단계 정확도·오탐율 집계
- 오탐 케이스 추가 수집 및 프롬프트 튜닝 결과 반영 확인
- 관리자 대시보드 데모 준비: 오탐 신고 승인/해제·HITL 검토 흐름 포함 시연 가능 상태 확인
```
