# 데일리 리포트 — 2026-07-20 (강민기)

## 오늘 한 일
- RAG 규정 지침 로직을 `llm_logic.py`에서 게임 서버(`game.py`)로 이식하고, `llm_logic.py` 내부 구조도 함께 정리했다.
- 제재 수위 결정 로직을 고도화했다. 기존 단순 누적 횟수 기반에서 위반 카테고리별 누적 횟수를 추적하는 방식으로 전환하고, 레벨·위반 차수에 따른 매핑 행렬(Matrix)로 처벌 수위(WARN/MUTE/BAN)와 기간을 결정하도록 변경했다.
- 저신뢰 케이스를 HITL 큐로 넘기는 분기 처리를 추가했다.
- 관리자 제재 및 AI 자동 제재 시 웹소켓 `ban_user`/`mute_user` 호출에 `reason` 인자를 추가해 클라이언트 측에 사유가 전달되도록 수정했다.

## 배운 점 / 특이사항
- 카테고리별 누적 집계를 위해 과거 제재 레코드의 `ai_result` JSON을 파싱하는 방식을 택했다. 데이터 정합성에 의존하는 구조이므로, 저장 포맷이 바뀌면 집계가 틀릴 수 있다는 점을 인지했다.
- Day 3 목표인 "신고 텍스트 → 제재 수위 반환까지 도는 최소 파이프라인" 구성은 오늘 커밋으로 완료했다.

## 내일 할 일
- RAG 검색 품질 점검 및 프롬프트 튜닝 (Day 4 목표)
- 오탐 케이스 수집 및 HITL 임계값 조정
- 카테고리별 누적 집계 로직 엣지 케이스 검증
