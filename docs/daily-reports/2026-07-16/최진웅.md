# 데일리 리포트 — 2026-07-16 (최진웡)

## 오늘 한 일
- FastAPI 백엔드와 게임 프론트엔드를 동일 포트(3000)에서 제공하도록 `StaticFiles` 마운트를 추가했다.
- WebRTC 시그널링(offer/answer/ICE candidate) 메시지를 발신자 제외 상대방에게만 릴레이하는 로직을 `game.py`와 `game_state.py`에 구현했다.
- 대기 화면에 플레이어 접속 상태 목록(나 / 상대 플레이어 대기 중)을 표시하는 UI를 `index.html`에 추가했다.
- 로그인 화면에서 비밀번호 필드를 제거하고 아이디만으로 입력하도록 `auth.js`와 `index.html`을 수정했다.
- Node 서버 코드를 `server/routes/voice.js`, `server/sockets/chat.js`, `server/state.js`로 분리해 구조를 정리하고, `admin.html`·`admin.css`를 게임 클라이언트 디렉터리에서 제거했다.

## 배운 점 / 특이사항
- 서버 포트 충돌 및 프로세스 잔존 문제를 반복적으로 겪었다(`.claude/settings.local.json`의 `taskkill` 커맨드 다수 추가로 확인됨).
- 백엔드와 프론트를 단일 포트로 통합하면서 정적 파일 마운트 순서가 라우터보다 뒤에 와야 한다는 점을 확인했다.

## 내일 할 일
- 계획상 7/17은 제헌절 휴일이므로 프로젝트 작업 없음
- (7/20 Day 3 준비) 오늘 구현한 WebRTC 릴레이 기반 위에 Azure AI Speech STT 연동을 붙여 신고 요청 → 음성 수집 → 변환 텍스트 반환까지 1회 완주하는 파이프라인 준비
