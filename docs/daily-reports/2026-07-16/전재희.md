# 데일리 리포트 — 2026-07-16 (전재희)

## 오늘 한 일
- `docs/team.md`에서 이준석 팀원의 GitHub 계정명을 `leejunseok` → `MyNameIsJUNSEOK`으로 수정했다.
- GitHub Actions 아침 브리핑 스케줄 크론식을 수정했다. KST 9시 기준이던 것을 GitHub 예약 지연을 감안해 08:20 KST(UTC 전날 23:20)로 앞당겼다.
- Claude 응답에서 리터럴 `\n` 문자열이 실제 개행으로 렌더되지 않는 문제를 수정했다. `normalize_breaks()` 함수를 추가해 `\r\n`, `\n`, `\r`를 모두 실제 개행으로 변환하도록 처리했다.
- Teams Adaptive Card 렌더링을 고려해 시스템 프롬프트 출력 규칙을 수정했다. 리터럴 `\n` 대신 실제 개행을 사용하도록 모델에 지시하는 문구를 명확히 했다.

## 배운 점 / 특이사항
- Teams Adaptive Card TextBlock은 리터럴 `\n` 문자열을 줄바꿈으로 인식하지 않으며, 실제 개행만 처리된다는 점을 확인했다.
- `team.md` 비고란에 "16일 이후 제외"로 기재되어 있어, 오늘이 프로젝트 참여 마지막 날이다.

## 내일 할 일
- 해당 없음 (16일 이후 프로젝트에서 제외)
