# 데일리 리포트 — 2026-07-16 (강민기)

## 오늘 한 일
- FastAPI 기반 백엔드 초반 구조를 구현했다. `database.py`, `main.py`, `models.py`, 라우터(`game.py`, `admin.py`), WebSocket 연결 관리(`game_state.py`) 등 기본 골격을 잡았다.
- 신고 API(`/api/report`)에 DB 연동을 완료했다. 신고자·피신고자 유저가 DB에 없을 경우 자동 생성하는 안전장치도 함께 추가했다.
- OpenAI API 호출 로직(`llm_logic.py`)을 구현하고, 신고 접수 후 백그라운드 태스크(`BackgroundTasks`)로 텍스트 채팅 분석 → 제재 수위 판정 → DB 저장 → WebSocket 실시간 강퇴까지 이어지는 파이프라인을 완성했다.
- 로그인 엔드포인트(`/api/login`)를 추가하고, `.gitignore`에 `__pycache__` 및 가상환경 항목을 추가해 불필요한 바이너리 파일을 정리했다.

## 배운 점 / 특이사항
- 신고 처리를 동기 방식으로 두면 응답 지연이 발생하므로 `BackgroundTasks`로 분리하는 것이 효과적이었다.
- 텍스트 채팅 신고 파이프라인은 1회 완주 가능한 상태까지 도달했다. 음성 채팅(STT) 연동은 현재 구현 중이며 프론트 코드 수정이 대기 상태다.

## 내일 할 일
- 계획표 기준 7/17은 제헌절(공휴일)로 프로젝트 없음
- 다음 작업일(7/20)을 위해 음성 채팅 신고 파이프라인(Azure AI Speech STT 연동) 흐름 정리 및 프론트 연동 사항 확인을 준비해둔다.
