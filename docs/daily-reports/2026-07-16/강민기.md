# 데일리 리포트 — 2026-07-16 (강민기)

## 오늘 한 일
- FastAPI 기반 백엔드 초기 구조를 구현했다. `main.py`, `database.py`, 모델, 라우터(`game`, `admin`), 서비스 레이어(`game_state`)를 신규 작성했다.
- 신고 API(`/api/report`)에 DB 연동을 완료했다. 신고 시 신고자·피신고자 User 레코드가 없을 경우 자동 생성하는 안전장치도 추가했다.
- 로그인 API(`/api/login`)를 구현했다. 최초 접속 시 User를 자동 생성하고 뮤트 상태를 반환하도록 처리했다.
- OpenAI API 호출 로직(`llm_logic.py`)을 구현했다. DB 스키마에 `content_text` 컬럼을 추가했고, 프론트엔드 연동은 대기 상태다.
- `.gitignore`에 `__pycache__`, `.pyc` 등 Python 빌드 산출물 패턴을 추가하고 기존에 트래킹되던 파일들을 삭제했다.

## 배운 점 / 특이사항
- 외래키 제약 에러를 방지하기 위해 신고 처리 전 유저 존재 여부를 확인하는 방어 로직이 필요했다.
- 프론트엔드 코드 수정이 아직 완료되지 않아 OpenAI API 연동의 엔드투엔드 검증은 미완 상태다.

## 내일 할 일
- 프론트엔드 코드 수정을 완료하고 OpenAI API 호출 흐름을 엔드투엔드로 검증한다.
- 전처리·청킹·임베딩 파이프라인을 구성해 문서에서 벡터 DB 적재까지 스크립트 1회 완주를 목표로 한다. (Day 2 목표)
