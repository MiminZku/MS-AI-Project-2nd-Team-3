```markdown
# 데일리 리포트 — 2026-07-14 (최진웅)

## 오늘 한 일
- `game_client` 디렉토리 전체를 초기 커밋(`game_client_ver.1`)으로 구성했다.
- 클라이언트 화면(`index.html`, `style.css`)과 관리자 페이지(`admin.html`, `admin.css`)를 작성했다.
- 게임 로직(`game.js`), AI 연동(`ai.js`), 음성(`voice.js`), 채팅(`chat.js`), 인증(`auth.js`), 리포트(`report.js`), 상태 관리(`state.js`), UI(`ui.js`) 모듈을 분리해 구현했다.
- WebSocket 기반 서버(`server.js`)를 구성하고 `ws` 패키지를 설치해 로컬 실행 환경을 세팅했다.

## 배운 점 / 특이사항
- 평가/테스트 파트 담당이지만 Day 1 킥오프 일정에 맞춰 클라이언트 환경 세팅 전반을 먼저 구성했다.
- `node_modules`가 커밋에 포함되어 있어, 추후 `.gitignore` 적용 여부를 팀과 확인할 필요가 있다.

## 내일 할 일
- 7/15는 멘토링 일정이므로, 멘토링 전까지 구현한 클라이언트가 서버와 정상 통신하는지 연결 동작을 검증한다.
- 평가 파트 준비로, Day 2(7/16) 파이프라인 완성 이후 적용할 평가 지표 후보(예: 답변 정확도, 근거 포함 여부)를 정리해 둔다.
```
