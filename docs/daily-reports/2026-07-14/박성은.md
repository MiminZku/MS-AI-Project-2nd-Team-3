# 데일리 리포트 — 2026-07-14 (박성은)

## 오늘 한 일
- 게임 채팅 제재 판단용 카테고리 데이터셋(`game_chat_categorized.txt`)을 2차 수정했다. 맥락상 정상 발화로 보아야 할 항목들의 레이블·카테고리를 `정상/0`으로 재분류했다.
- OpenAI API를 활용한 1차 욕설 탐지 PoC 스크립트(`test_openai_moderation.py`, `test_categorized.py`)와 초기 데이터셋(`curse_detection_data.txt`, `game_chat_test.txt`)을 레포에 추가했다.
- `test_categorized.py`에 `max_output_tokens` 및 `reasoning effort` 파라미터를 추가해 빈 응답 문제를 방지했고, 동점 카테고리가 발생할 때 팀 정의 우선순위(폭력성 우선 등)로 대표 카테고리를 결정하는 로직을 추가했다.

## 배운 점 / 특이사항
- `reasoning` 모드에서 내부 사고 토큰이 최종 응답 토큰을 잠식해 빈 출력이 발생할 수 있다는 점을 확인했다. `reasoning={"effort": "low"}`와 `max_output_tokens` 여유 확보로 해결했다.
- 레이블 기준이 모호한 경계 케이스(단순 감정 표출, 맥락 없는 불편 표현 등)는 정상으로 분류하는 방향으로 데이터셋 기준을 정비했다.

## 내일 할 일
- plan.md Day 2(7/16) 대비 사전 작업으로, 오늘 정비한 데이터셋을 기반으로 청킹·임베딩 파이프라인 설계를 시작한다.
- PoC 테스트 결과를 정리해 팀과 공유하고, 제재 카테고리 기준 및 우선순위 정의를 팀 내에서 합의한다.
