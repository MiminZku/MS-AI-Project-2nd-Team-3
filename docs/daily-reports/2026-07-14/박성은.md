# 데일리 리포트 — 2026-07-14 (박성은)

## 오늘 한 일
- 게임 채팅 제재 판단 데이터셋(`game_chat_categorized.txt`)을 2차 수정했다. 맥락상 경계가 모호한 샘플들을 재검토해 레이블과 카테고리를 정상으로 조정했다.
- OpenAI API를 활용한 제재 분류 PoC 스크립트(`test_categorized.py`)를 작성하고 1차 테스트를 진행했다. `max_output_tokens`와 `reasoning effort` 파라미터를 조정해 빈 응답 문제를 완화했다.
- 여러 카테고리가 동점일 때 대표 카테고리를 결정하는 우선순위 로직을 추가하기 시작했다(폭력성 최우선 기준 초안).

## 배운 점 / 특이사항
- `reasoning` 모델에서 내부 사고 토큰이 과도하게 소비되면 최종 응답이 비어버리는 현상이 있었다. `reasoning={"effort": "low"}`로 줄이는 것이 효과적이었다.
- 경계선 발화(예: "확 다 갈아버릴까", "이상한 소리 좀 그만해라")의 레이블 기준을 팀 내에서 명확히 합의할 필요가 있다.

## 내일 할 일
- 제재 카테고리 우선순위 로직 완성 및 분류 정확도 측정
- 청킹·임베딩 파이프라인 작업 본격 착수 (Day 2 목표: 문서 → 벡터 DB 적재 1회 완주)
- 전처리 스크립트와 임베딩 연결 구조 초안 작성
