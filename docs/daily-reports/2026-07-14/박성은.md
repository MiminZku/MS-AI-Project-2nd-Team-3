# 데일리 리포트 — 2026-07-14 (박성은)

## 오늘 한 일
- 게임 채팅 제재 판단용 카테고리 데이터셋(`game_chat_categorized.txt`)을 2차 수정했다. 경계가 모호한 발화(감정 표출, 맥락 없는 반응성 발언 등)를 `정상(label=0)` 으로 재분류하여 라벨 기준을 정비했다.
- `test_categorized.py`에 `max_output_tokens`와 `reasoning effort` 파라미터를 추가해 모델의 빈 응답 문제를 방지했다.
- 동점 카테고리가 나올 때 팀 우선순위(폭력성 우선 등)로 대표 카테고리를 결정하는 로직을 추가하기 시작했다.
- OpenAI Moderation API 1차 PoC를 완성하여 커밋했다. 원본 욕설 탐지 데이터셋(`curse_detection_data.txt`, 5800여 건), 카테고리별 테스트 스크립트 2종, 샘플 데이터를 레포에 처음으로 올렸다.

## 배운 점 / 특이사항
- `reasoning effort`를 줄이면 내부 사고 토큰 소모가 줄어 최종 응답 토큰 여유가 생긴다는 점을 실험으로 확인했다.
- 라벨 기준을 명확히 하지 않으면 경계 발화가 모델 성능 평가를 왜곡할 수 있어, 데이터 정제가 파이프라인 품질에 선행함을 체감했다.

## 내일 할 일
- 카테고리 동점 시 우선순위 로직을 완성하고 전체 테스트 케이스 기준으로 정확도를 측정한다.
- Day 2 목표(전처리 → 청킹 → 임베딩 → 벡터 DB 적재 1회 완주)에 맞춰 청킹/임베딩 파이프라인 스크립트 작성을 시작한다.
