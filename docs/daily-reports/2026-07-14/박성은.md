# 데일리 리포트 — 2026-07-14 (박성은)

## 오늘 한 일
- 게임 채팅 유해발언 분류 PoC(`moderation-poc`)를 초기 구성했다. OpenAI 모델을 활용한 분류 스크립트(`test_categorized.py`, `test_openai_moderation.py`)와 테스트 데이터셋을 1차 커밋했다.
- 데이터셋(`game_chat_categorized.txt`) 라벨을 2차 수정했다. 경계선 케이스(경미한 감정 표출, 맥락상 정상 표현 등)를 `label=0 / 정상` 으로 재분류해 기준을 명확히 했다.
- 빈 응답 문제에 대응하기 위해 `max_output_tokens` 및 `reasoning effort` 파라미터를 추가하고, 빈 응답을 명시적 예외(`ValueError`)로 처리한 뒤 재시도하는 `classify_with_retry` 함수를 구현했다.
- 동점 카테고리 발생 시 팀 정의 우선순위(폭력성 우선 등)로 대표 카테고리를 결정하는 제재 우선 로직을 추가했다.

## 배운 점 / 특이사항
- `gpt-5-mini` 추론 모델에서 `max_output_tokens`이 부족하면 빈 응답이 발생할 수 있음을 확인했다. `reasoning effort`를 `low`로 설정해 최종 출력 토큰 여유를 확보하는 방식으로 우회했다.
- 데이터셋 파일명에 `_DRAFT` 접미사를 붙여 아직 확정되지 않은 상태임을 명시했다.

## 내일 할 일
- 오늘 구축한 분류 스크립트를 기반으로 전처리 → 청킹 → 임베딩 파이프라인 작업을 시작한다 (Day 2 목표).
- `curse_detection_data.txt` 등 확보된 데이터를 정제·청킹해 벡터 DB에 1회 적재하는 스크립트를 완주하는 것을 목표로 한다.
- 데이터셋 DRAFT 상태를 팀과 검토해 라벨 기준을 확정한다.
