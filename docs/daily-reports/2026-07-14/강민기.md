# 데일리 리포트 — 2026-07-14 (강민기)

## 오늘 한 일
- 로컬 Windows 환경에 PostgreSQL을 직접 설치하고, `apple_game` 데이터베이스 및 테이블 스키마(`init.sql`)를 구성했다.
- 내부망(172.16.30.143) 공유 설정과 Windows 방화벽 포트 개방을 완료해 팀원들이 같은 와이파이 내에서 DB에 접속할 수 있도록 했다.
- DB 연결 테스트 스크립트(`test_db.py`), `.env.example`, README를 작성하고 레포에 커밋했다.
- 설치 과정 전체를 `PostgreSQL_Setup_Guide.md`로 문서화하여 `study/` 폴더로 정리했다.

## 배운 점 / 특이사항
- Docker 가상화 이슈를 피하기 위해 PostgreSQL 직접 설치 방식을 채택했고, 이를 `docs/decisions.md`에 기록했다.
- `.env` 파일에 실제 비밀번호가 노출된 커밋이 있었고, 이후 해당 예시를 삭제하고 `.gitignore`를 전역으로 수정해 재발을 방지했다.

## 내일 할 일
- Day 2(7/16) 작업 준비로 문서 전처리 및 청킹 스크립트 초안 작성을 시작한다.
- 수집 대상 원본 문서 포맷을 확인하고 파싱 방식을 결정한다.
