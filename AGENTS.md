# AGENTS.md

이 레포는 6일 팀 프로젝트(6인)입니다. 모든 팀원의 AI가 같은 컨텍스트에서 출발하도록, 아래 규칙을 반드시 따르세요.

## 세션 시작 시 (필수)

작업을 시작하기 전에 다음 파일을 먼저 읽고, 그 내용을 전제로 작업한다:

1. `docs/PROJECT.md` — 프로젝트 방향과 확정된 기술 결정 (헌법)
2. `docs/decisions.md` — 최근 결정 로그 (특히 마지막 5개)
3. `docs/plan.md` — 오늘이 Day 몇인지, 오늘의 목표
a;skdljf;lskadjf
PROJECT.md의 "만들지 않는 것"에 해당하는 기능은 제안하거나 구현하지 않는다.
PROJECT.md의 확정 결정과 다른 기술을 쓰자는 요청을 받으면, 진행하기 전에 "결정사항과 다른데 팀 합의된 변경인지" 사용자에게 확인한다.

## 결정 기록 (자동)

대화 중 프로젝트 방향이나 기술 선택이 **새로 정해지거나 바뀌면**, 사용자에게 짧게 확인 후 `docs/decisions.md` 맨 아래에 한 줄 추가하고 커밋한다:

```
- [YYYY-MM-DD] [이름] 결정 내용 — 이유
```

확정 결정이 바뀐 경우 `docs/PROJECT.md`의 표도 함께 갱신한다.

## 세션 마감

사용자가 "마감" 또는 "마감해줘"라고 하면 `docs/rules/closing-rule.md`의 절차를 따른다.

## 커밋/푸시 공통 규칙

- 푸시 전 항상 `git pull --rebase`.
- docs/ 아래 파일 충돌 시, 추가 전용 파일(decisions.md, ai-log/)은 양쪽을 모두 살려 해결한다.
