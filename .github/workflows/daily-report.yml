name: daily-report

on:
  schedule:
    - cron: '40 8 16,20,21,22,23,24 7 *'   # 프로젝트 일자 + 발표일(24) 오후 5시 40분(KST) = UTC 08:40
  workflow_dispatch: {}   # 수동 전용: Actions 탭 → daily-report → Run workflow 버튼으로 실행

permissions:
  contents: write           # 리포트 파일을 레포에 커밋하기 위해 필요

concurrency:
  group: daily-report-${{ github.ref }}
  cancel-in-progress: false   # 겹치면 취소 말고 대기 → 앞 실행이 push 끝낸 뒤 순서대로 실행

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch all branches
        run: git fetch --all --prune

      - name: Generate daily reports
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
        run: python3 scripts/daily_report.py

      - name: Commit reports
        run: |
          git config user.name "daily-report-bot"
          git config user.email "bot@users.noreply.github.com"
          git add docs/daily-reports/
          if git diff --cached --quiet; then
            echo "커밋할 리포트 없음"
          else
            git commit -m "report: $(TZ=Asia/Seoul date +%F) 데일리 리포트 자동 생성"
            git pull --rebase origin ${GITHUB_REF_NAME}
            git push
          fi
